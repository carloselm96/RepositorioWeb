@model IEnumerable<EventoDeportivo.Models.Partido>

@{
    ViewData["Title"] = "Partidos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<p>
    <a asp-action="Create">Create New</a>
</p>
<h1>Tabla de Partidos</h1>
<center>
    <table class="table table-striped table-hover" id="data_table1">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Hora</th>
                <th>Contrincante</th>
                <th>Contrincante</th>
                <th>Disciplina</th>
                <th></th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <th>Id</th>
                <th>Fecha</th>                
                <th>Contrincante</th>
                <th>Contrincante</th>
                <th>Disciplina</th>
                <th></th>
            </tr>
        </tfoot>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.fecha_hora)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.equipo1.nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.equipo2.nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.disciplina.nombre)
                    </td>x
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</center>
<p>
    <a href="#">@Html.ActionLink("Regresar", "Index")</a>
</p>
<script>
    $(document).ready(function () {
        $('#data_table1').DataTable();
    });
</script>
